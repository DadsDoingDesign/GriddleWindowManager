{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions (overlay webviews are excluded — they get the narrower `overlay` capability)",
  "windows": [
    "main",
    "settings"
  ],
  "permissions": [
    "core:default"
  ]
}
